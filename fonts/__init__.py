"""Faithful-ish text styling. You can only RENDER with installed fonts, so
"matching" = sample the real color + size from the original, and pick the
closest available font/weight by ink-density (render-and-compare). Not arbitrary
font identification — the practical, usable form.
"""
